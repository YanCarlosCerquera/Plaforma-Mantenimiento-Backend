import { ActionLogInterceptor } from './action-log.interceptor';

describe('ActionLogInterceptor', () => {
  it('should be defined', () => {
    expect(new ActionLogInterceptor()).toBeDefined();
  });
});
